# =======================================================
# Struktura Menu (BEZ TEKSTÓW TŁUMACZEŃ)
# Wzorowane na działającym pliku użytkownika
# =======================================================

# 1. Definiujemy, które opcje są suwakami
sliders = GAMMA GLOBAL_SPEED HEIGHT_HIGH HEIGHT_MID HEIGHT_LOW BLOCK_SIZE_HIGH BLOCK_SIZE_MID BLOCK_SIZE_LOW WEATHER_VIGNETTE WEATHER_DESATURATION FOG_DENSITY_RAIN FOG_DENSITY_THUNDER

# 2. Główny ekran - tworzymy przyciski (zakładki) w nawiasach kwadratowych []
screen = [POST_PROCESSING] [FLUIDS] [SKY_AND_CLOUDS] [WEATHER] *
screen.columns = 2

# --- ZAWARTOŚĆ ZAKŁADEK ---

screen.POST_PROCESSING = TONEMAP_MODE COLOR_PROFILE GAMMA
screen.POST_PROCESSING.columns = 1

screen.FLUIDS = WATER_STYLE LAVA_STYLE
screen.FLUIDS.columns = 1

# Dodane <empty>, żeby przełącznik, wysokość i skala układały się w 3 równe kolumny
screen.SKY_AND_CLOUDS = GLOBAL_SPEED <empty> <empty> ENABLE_HIGH_CLOUDS HEIGHT_HIGH BLOCK_SIZE_HIGH ENABLE_MID_CLOUDS HEIGHT_MID BLOCK_SIZE_MID ENABLE_LOW_CLOUDS HEIGHT_LOW BLOCK_SIZE_LOW
screen.SKY_AND_CLOUDS.columns = 3

screen.WEATHER = WEATHER_VIGNETTE WEATHER_DESATURATION FOG_DENSITY_RAIN FOG_DENSITY_THUNDER
screen.WEATHER.columns = 1